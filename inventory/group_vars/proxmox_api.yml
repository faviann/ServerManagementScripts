---
ansible_connection: local
ansible_python_interpreter: "{{ ansible_playbook_python }}"

# Plays targeting this group should set gather_facts: false to avoid needless SSH.
# API credentials and defaults are defined in group_vars/all/proxmox.yml so they
# are available to every host (including LXCs) during delegation.
